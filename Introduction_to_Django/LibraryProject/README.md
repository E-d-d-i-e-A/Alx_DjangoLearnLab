# LibraryProject

A Django project created for the ALX Django Learning Lab.

## Setup
1. Install Django: `pip install django`
2. Run server: `python manage.py runserver`
3. Visit: http://127.0.0.1:8000/
```
